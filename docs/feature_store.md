# Feature Store

## Purpose

Reusable, reproducible quantitative features for risk modeling and backtesting. All features are produced by DVC stages and stored as parquet under `data/features/`.

## Feature modules

| Module | Outputs | Description |
|--------|---------|-------------|
| `returns` | `returns`, `log_returns` | Simple and log returns per ticker |
| `volatility` | `volatility`, `rolling_vol_*` | Rolling std, annualized volatility |
| `risk_ratios` | `rolling_sharpe` | Rolling Sharpe vs config risk-free rate |
| `technical` | `sma_*`, `momentum`, `drawdown` | Technical indicators |
| `liquidity` | `volume_zscore` | Volume z-score (written in volatility stage) |
| `correlation` | `rolling_corr_*` | Rolling correlation vs benchmark ticker |
| `sentiment_agg` | `confidence`, `bullish_ratio`, `article_count` | Daily aggregated FinBERT scores |

## Sentiment ↔ market bridge

Phase 1 news lacks tickers. Mapping via [`configs/sentiment_map.yaml`](../configs/sentiment_map.yaml):

- `source_to_ticker`: map news source → equity ticker
- `default_ticker`: `MARKET` for unmapped articles

## Merged risk dataset

`merge_features` performs a left-join chain on `(timestamp, ticker)`:

1. Returns (base)
2. Volatility features
3. Technical features
4. Sentiment aggregates

Output: `data/features/merged/risk_dataset.parquet` plus `_metadata.json` with `feature_version` and column list.

Plotly overview (optional): `data/analytics/risk_dataset_overview.html` (generated by `merge_features`).

## Future use

- **Batch backtesting:** scan merged parquet lazily by date range
- **Live inference:** same functions on streaming LazyFrames with bounded windows
- **Phase 2b:** VaR/CVaR modules in `src/risk/` consume `risk_dataset`
