# Core (mirrors pyproject.toml)
# Polars >= 1.20 enables LazyFrame.sink_parquet(PartitionBy(...)) for partitioned writes
polars>=1.20.0
pyarrow>=15.0.0
numpy>=1.24.0
transformers>=4.45.0
torch>=2.0.0
pyyaml>=6.0
dvclive>=3.0.0
dvc>=3.0.0

# Install: pip install -e ".[dev,market,notebook]"
