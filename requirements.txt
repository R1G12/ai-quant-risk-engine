# Core pipeline (mirrors pyproject.toml [project.dependencies])
polars>=0.20.0
numpy>=1.24.0
transformers>=4.45.0
torch>=2.0.0
pyyaml>=6.0
dvclive>=3.0.0
dvc>=3.0.0

# Editable install: pip install -e ".[dev,notebook]"
